import React from 'react';
import useAuth from '../../hooks/useAuth';

const MyBlog = () => {
    const { user } = useAuth()
    return (
        <div>

        </div>
    );
};

export default MyBlog;